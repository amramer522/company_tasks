import 'package:flutter/material.dart';

class TestSocketView extends StatelessWidget {
  const TestSocketView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
